export type LocalizedText = {
  en: string;
  zh: string;
  th: string;
};

export type TripDetailMore = {
  location: string;
  lat: number;
  lng: number;
  img: string;
  gallery: string[];
  video: string;
  credit: string;
};
